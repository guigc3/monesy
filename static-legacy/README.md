# Frontend legado (arquivado)

Esta pasta contém o SPA original em HTML + JavaScript vanilla (`index.html`, `app.js`, `auth.js`, etc.).

O frontend ativo é **Vue 3 + Vite** em [`../frontend/`](../frontend/). O Flask serve `frontend/dist/` em produção.

Mantido apenas como referência histórica e fallback se `frontend/dist/` não existir.
